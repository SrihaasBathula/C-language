#include<stdio.h>
void main()
{
    int a,sum;
    printf("Enter a Number:");
    scanf("%d",&a);
    sum = 0;
    
    while(a>=1)
    {
        sum=sum+(a%10);
        a=a/10;
    }
    printf("%d is the sum of the digits",sum);
}    
